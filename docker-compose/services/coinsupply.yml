version: '3.9'

services:
  coinsupply:
    image: stratisevm/coinsupply:latest
    pull_policy: always
    platform: linux/amd64
    restart: always
    container_name: 'coinsupply'
    command: --eth-rpc-url https://auroria.rpc.stratisevm.com --eth2-rpc-url https://auroria.rpc.stratisevm.com/prysm --explorer-url https://auroria.explorer.stratisevm.com
