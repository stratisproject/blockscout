version: '3.9'

services:
  coinsupply:
    image: stratisevm/coinsupply:latest
    pull_policy: always
    platform: linux/amd64
    restart: always
    container_name: 'coinsupply'
    command: --eth-rpc-url http://172.17.0.1:8545 --eth2-rpc-url http://172.17.0.1:3500 --explorer-url http://backend:4000
